# Kubernetes ConfigMap

```bash
$ helm secrets upgrade app app-python/ -n default -f secrets.yaml
[helm-secrets] Decrypt: secrets.yaml
Release "app" has been upgraded. Happy Helming!
NAME: app
LAST DEPLOYED: Sun Mar 23 09:22:31 2025
NAMESPACE: default
STATUS: deployed
REVISION: 6
NOTES:
1. Get the application URL by running these commands:
     NOTE: It may take a few minutes for the LoadBalancer IP to be available.
           You can watch its status by running 'kubectl get --namespace default svc -w app-app-python'
  export SERVICE_IP=$(kubectl get svc --namespace default app-app-python --template "{{ range (index .status.loadBalancer.ingress 0) }}{{.}}{{ end }}")
  echo http://$SERVICE_IP:8080
[helm-secrets] Removed: secrets.yaml.dec
```

```bash
$ kubectl get po
NAME                              READY   STATUS    RESTARTS   AGE
app-app-python-5c9fc49fcd-7bqrd   1/1     Running   0          6s
```

```bash
$ kubectl exec app-app-python-5c9fc49fcd-7bqrd -- cat /config/config.json

{
    "hello": "world"
}
```

(mounted to `/config` folder inside the pod)
